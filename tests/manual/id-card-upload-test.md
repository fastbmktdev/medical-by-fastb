# Manual Test Guide: ID Card Upload with Watermark

## 🎯 Test Objectives
ทดสอบระบบอัปโหลดบัตรประชาชนพร้อมลายน้ำสำหรับการสมัครเป็น Partner

---

## 📋 Pre-requisites
1. ✅ Database migration รันแล้ว (`20251118000000_add_id_card_columns.sql`)
2. ✅ Supabase Storage bucket `gym-images` พร้อมใช้งาน
3. ✅ User account สำหรับทดสอบ (ต้อง login)
4. ✅ รูปภาพบัตรประชาชนสำหรับทดสอบ (JPG/PNG, < 10MB)

---

## 🧪 Test Cases

### Test Case 1: อัปโหลดบัตรประชาชนสำเร็จ
**Steps:**
1. ไปที่หน้า `/th/partner/apply` (หรือ locale อื่น)
2. Login ด้วย user account
3. กรอกข้อมูลฟอร์มสมัคร partner
4. Scroll ไปที่ส่วน "บัตรประชาชน (จำเป็น)"
5. คลิกที่พื้นที่อัปโหลด
6. เลือกไฟล์รูปภาพบัตรประชาชน (JPG หรือ PNG)
7. รอให้ระบบประมวลผลและเพิ่มลายน้ำ
8. รอให้อัปโหลดเสร็จ

**Expected Results:**
- ✅ แสดง progress indicator "กำลังประมวลผลภาพ..."
- ✅ แสดง progress indicator "กำลังอัปโหลด..."
- ✅ แสดง preview รูปภาพที่มีลายน้ำ
- ✅ มี badge "มีลายน้ำ" ที่มุมขวาบนของรูป
- ✅ แสดงข้อความ "อัปโหลดสำเร็จ"
- ✅ Toast notification แสดง "อัปโหลดบัตรประชาชนสำเร็จ"

**Verify in Database:**
```sql
SELECT id_card_url, id_card_original_url 
FROM gyms 
WHERE user_id = '<test_user_id>' 
ORDER BY created_at DESC 
LIMIT 1;
```
- ✅ `id_card_url` ต้องมีค่า (URL ของรูปที่มีลายน้ำ)
- ✅ `id_card_original_url` ต้องมีค่า (URL ของรูปต้นฉบับ)

**Verify in Storage:**
- ✅ ไฟล์ต้นฉบับ: `id-cards/{user_id}/original_{timestamp}_{random}.{ext}`
- ✅ ไฟล์มีลายน้ำ: `id-cards/{user_id}/watermarked_{timestamp}_{random}.{ext}`

---

### Test Case 2: Validation - ไฟล์ใหญ่เกินไป
**Steps:**
1. ไปที่หน้า `/th/partner/apply`
2. พยายามอัปโหลดไฟล์ที่ใหญ่กว่า 10MB

**Expected Results:**
- ✅ แสดง error message "ขนาดไฟล์ต้องไม่เกิน 10MB"
- ✅ Toast notification แสดง error
- ✅ ไม่มีการอัปโหลดไฟล์

---

### Test Case 3: Validation - ไฟล์เล็กเกินไป
**Steps:**
1. ไปที่หน้า `/th/partner/apply`
2. พยายามอัปโหลดไฟล์ที่เล็กกว่า 50KB

**Expected Results:**
- ✅ แสดง error message "ไฟล์มีขนาดเล็กเกินไป..."
- ✅ Toast notification แสดง error
- ✅ ไม่มีการอัปโหลดไฟล์

---

### Test Case 4: Validation - ไฟล์ประเภทไม่ถูกต้อง
**Steps:**
1. ไปที่หน้า `/th/partner/apply`
2. พยายามอัปโหลดไฟล์ PDF หรือไฟล์อื่นที่ไม่ใช่ JPG/PNG

**Expected Results:**
- ✅ แสดง error message "รองรับเฉพาะไฟล์ JPG และ PNG เท่านั้น"
- ✅ Toast notification แสดง error
- ✅ ไม่มีการอัปโหลดไฟล์

---

### Test Case 5: ลบและอัปโหลดใหม่
**Steps:**
1. อัปโหลดบัตรประชาชนสำเร็จแล้ว
2. คลิกปุ่ม "ลบและอัปโหลดใหม่"

**Expected Results:**
- ✅ รูป preview หายไป
- ✅ กลับไปแสดงพื้นที่อัปโหลด
- ✅ สามารถอัปโหลดไฟล์ใหม่ได้

---

### Test Case 6: ส่งใบสมัครโดยไม่อัปโหลดบัตรประชาชน
**Steps:**
1. กรอกข้อมูลฟอร์มสมัคร partner ทั้งหมด
2. **ไม่** อัปโหลดบัตรประชาชน
3. คลิกปุ่ม "สมัครเข้าร่วมเป็น Partner"

**Expected Results:**
- ✅ แสดง error message "กรุณาอัปโหลดบัตรประชาชน"
- ✅ ไม่สามารถส่งใบสมัครได้
- ✅ Form ไม่ถูก submit

---

### Test Case 7: ส่งใบสมัครพร้อมบัตรประชาชน
**Steps:**
1. กรอกข้อมูลฟอร์มสมัคร partner ทั้งหมด
2. อัปโหลดบัตรประชาชนสำเร็จ
3. คลิกปุ่ม "สมัครเข้าร่วมเป็น Partner"
4. ยอมรับ Terms & Conditions

**Expected Results:**
- ✅ ใบสมัครถูกส่งสำเร็จ
- ✅ ข้อมูลถูกบันทึกใน database พร้อม `id_card_url` และ `id_card_original_url`
- ✅ แสดงหน้า Success

**Verify in Database:**
```sql
SELECT 
  gym_name,
  id_card_url,
  id_card_original_url,
  status
FROM gyms 
WHERE user_id = '<test_user_id>' 
ORDER BY created_at DESC 
LIMIT 1;
```
- ✅ ข้อมูลถูกบันทึกครบถ้วน
- ✅ `status` = 'pending'

---

### Test Case 8: Admin ดูบัตรประชาชนในหน้าอนุมัติ
**Steps:**
1. Login เป็น Admin
2. ไปที่หน้า `/th/admin/dashboard/approvals`
3. คลิก "ดูรายละเอียด" ของใบสมัครที่รออนุมัติ
4. ตรวจสอบส่วน "บัตรประชาชน"

**Expected Results:**
- ✅ แสดงรูปบัตรประชาชนแบบมีลายน้ำเป็นค่าเริ่มต้น
- ✅ มีข้อความ "แบบมีลายน้ำ (สำหรับตรวจสอบ):"
- ✅ มี `<details>` dropdown สำหรับดูบัตรต้นฉบับ
- ✅ เมื่อคลิกดูบัตรต้นฉบับ:
  - ✅ แสดงคำเตือนสีเหลือง "⚠️ นี่คือบัตรประชาชนต้นฉบับ..."
  - ✅ แสดงรูปบัตรต้นฉบับ (ไม่มีลายน้ำ)
  - ✅ รูปชัดเจน อ่านได้

---

### Test Case 9: ตรวจสอบลายน้ำ
**Steps:**
1. อัปโหลดบัตรประชาชนสำเร็จ
2. ดู preview รูปภาพที่มีลายน้ำ

**Expected Results:**
- ✅ มีลายน้ำข้อความ "THAIKICK - สำหรับการสมัครเท่านั้น" แบบ grid pattern
- ✅ ลายน้ำหมุน -30 องศา
- ✅ มี timestamp วันที่สมัครที่ด้านล่าง
- ✅ ลายน้ำไม่บังข้อมูลสำคัญบนบัตรมากเกินไป
- ✅ ยังอ่านข้อมูลบนบัตรได้

---

### Test Case 10: API Endpoint - Upload
**Steps:**
1. ใช้ Postman หรือ curl
2. POST ไปที่ `/api/partner/id-card`
3. ส่ง FormData พร้อม:
   - `file`: ไฟล์ต้นฉบับ
   - `watermarkedFile`: ไฟล์ที่มีลายน้ำ

**Expected Results:**
- ✅ Status 200 OK
- ✅ Response:
  ```json
  {
    "success": true,
    "data": {
      "originalUrl": "...",
      "watermarkedUrl": "...",
      "originalPath": "...",
      "watermarkedPath": "..."
    }
  }
  ```

---

### Test Case 11: API Endpoint - Unauthorized
**Steps:**
1. ไม่ login
2. POST ไปที่ `/api/partner/id-card`

**Expected Results:**
- ✅ Status 401 Unauthorized
- ✅ Response: `{ "success": false, "error": "Unauthorized" }`

---

## 🔍 Additional Checks

### Browser Console
- ✅ ไม่มี JavaScript errors
- ✅ Network requests สำเร็จ (200 status)

### Network Tab
- ✅ POST `/api/partner/id-card` สำเร็จ
- ✅ ไฟล์ถูกอัปโหลดไปยัง Supabase Storage

### Database
- ✅ Columns `id_card_url` และ `id_card_original_url` มีค่า
- ✅ URLs ชี้ไปยังไฟล์ที่ถูกต้องใน Storage

---

## 🐛 Known Issues / Edge Cases to Test

1. **Slow Network**: ทดสอบด้วย Network throttling (Slow 3G)
2. **Large File**: ทดสอบด้วยไฟล์ขนาด ~9.5MB (ใกล้ขีดจำกัด)
3. **Multiple Uploads**: อัปโหลดหลายครั้งติดกัน
4. **Cancel Upload**: ยกเลิกการอัปโหลดระหว่างดำเนินการ
5. **Browser Compatibility**: ทดสอบใน Chrome, Firefox, Safari

---

## ✅ Test Checklist

- [ ] Test Case 1: อัปโหลดสำเร็จ
- [ ] Test Case 2: Validation - ไฟล์ใหญ่เกินไป
- [ ] Test Case 3: Validation - ไฟล์เล็กเกินไป
- [ ] Test Case 4: Validation - ไฟล์ประเภทไม่ถูกต้อง
- [ ] Test Case 5: ลบและอัปโหลดใหม่
- [ ] Test Case 6: ส่งใบสมัครโดยไม่อัปโหลดบัตร
- [ ] Test Case 7: ส่งใบสมัครพร้อมบัตรประชาชน
- [ ] Test Case 8: Admin ดูบัตรประชาชน
- [ ] Test Case 9: ตรวจสอบลายน้ำ
- [ ] Test Case 10: API Endpoint - Upload
- [ ] Test Case 11: API Endpoint - Unauthorized

---

## 📝 Notes

- ใช้รูปภาพบัตรประชาชนจริงหรือรูปทดสอบที่ชัดเจน
- ตรวจสอบว่า Supabase Storage bucket `gym-images` มี policy ที่ถูกต้อง
- ตรวจสอบ RLS policies สำหรับ `gyms` table

