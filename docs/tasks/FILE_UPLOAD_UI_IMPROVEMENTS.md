# File Upload UI Improvements

**วันที่**: 2025-11-06
**Feature**: Partner Application - Gym Image Upload

---

## ✅ การปรับปรุงที่ทำแล้ว

### 1. **ข้อความข้อกำหนดไฟล์ที่ชัดเจนขึ้น** ✅

#### Before:
```tsx
<p className="mb-3 text-zinc-500 text-xs">
  รองรับไฟล์: JPG, PNG (ขนาดไม่เกิน 5MB ต่อไฟล์)
</p>
```

#### After:
```tsx
<label className="block mb-2 font-medium text-zinc-300 text-sm">
  อัปโหลดรูปภาพยิม / โลโก้ <span className="text-zinc-500 font-normal">(ไม่บังคับ)</span>
</label>
<div className="mb-3 p-3 border border-blue-500/30 bg-blue-500/10 rounded-lg">
  <p className="text-blue-300 text-xs font-medium mb-1">
    📸 ข้อกำหนดไฟล์:
  </p>
  <ul className="text-blue-200/80 text-xs space-y-0.5 ml-4 list-disc">
    <li>ประเภทไฟล์: JPG, PNG, WebP</li>
    <li>ขนาดไฟล์: <strong className="text-blue-200">ไม่เกิน 5MB ต่อไฟล์</strong></li>
    <li>ความละเอียดแนะนำ: 800x600 px ขึ้นไป</li>
    <li>จำนวน: ไม่จำกัด (แนะนำ 3-5 รูป)</li>
  </ul>
</div>
```

**ประโยชน์**:
- 📌 เห็นข้อกำหนดชัดเจนทันที
- 📌 สีฟ้าทำให้เด่นและดึงดูดสายตา
- 📌 แสดงข้อมูลครบถ้วน (ประเภท, ขนาด, ความละเอียด, จำนวน)
- 📌 ระบุว่า "(ไม่บังคับ)" เพื่อความชัดเจน

---

### 2. **การแสดงขนาดไฟล์ที่ดีขึ้น** ✅

#### Before:
```tsx
<span className="text-zinc-500 text-xs">
  ({(file.size / 1024).toFixed(1)} KB)
</span>
```
แสดงเป็น KB เสมอ แม้ไฟล์จะใหญ่หลาย MB

#### After:
```tsx
<span className="text-xs">
  ({file.size >= 1024 * 1024
    ? `${fileSizeMB.toFixed(2)} MB`
    : `${(file.size / 1024).toFixed(1)} KB`})
</span>
```

**ประโยชน์**:
- 📌 แสดง KB สำหรับไฟล์เล็ก (< 1MB)
- 📌 แสดง MB สำหรับไฟล์ใหญ่ (>= 1MB)
- 📌 อ่านง่าย เข้าใจง่าย

---

### 3. **Visual Indicators สำหรับขนาดไฟล์** ✅

#### สีและสัญลักษณ์ตามขนาด:

**ไฟล์ปกติ (< 4MB)**:
```tsx
className="bg-zinc-700"  // สีเทาปกติ
text-zinc-500           // ข้อความสีเทา
```

**ไฟล์ใหญ่ (4-5MB)** - คำเตือน:
```tsx
className="bg-yellow-900/20 border border-yellow-500/50"  // สีเหลืองเตือน
text-yellow-400 font-medium                               // ข้อความสีเหลือง
```

**ไฟล์เกินขนาด (> 5MB)** - ข้อผิดพลาด:
```tsx
className="bg-red-900/30 border border-red-500"  // สีแดงแจ้งเตือน
text-red-400 font-semibold                       // ข้อความสีแดง
" - เกินขนาด!"                                   // ข้อความเตือนเพิ่มเติม
```

**ประโยชน์**:
- 🟢 **เขียว/เทา**: ไฟล์ OK
- 🟡 **เหลือง**: ไฟล์ใหญ่ แต่ยังใช้ได้ (คำเตือน)
- 🔴 **แดง**: ไฟล์เกินขนาด ไม่สามารถอัปโหลดได้

---

## 📊 ตัวอย่างการแสดงผล

### ไฟล์ปกติ (2.5 MB):
```
┌────────────────────────────────────────────┐
│ 🖼️  gym-photo-1.jpg    (2.50 MB)    [ลบ] │
└────────────────────────────────────────────┘
  สีเทา (bg-zinc-700)
```

### ไฟล์ใหญ่ (4.2 MB):
```
┌────────────────────────────────────────────┐
│ 🖼️  gym-photo-2.jpg    (4.20 MB)    [ลบ] │
└────────────────────────────────────────────┘
  สีเหลือง (bg-yellow-900/20) - คำเตือน
```

### ไฟล์เกินขนาด (6.5 MB):
```
┌────────────────────────────────────────────┐
│ 🖼️  gym-photo-3.jpg  (6.50 MB - เกินขนาด!) [ลบ] │
└────────────────────────────────────────────┘
  สีแดง (bg-red-900/30) - ข้อผิดพลาด
```

---

## 🎨 Color Scheme

| สถานะ | Background | Border | Text | Icon |
|-------|-----------|---------|------|------|
| **ปกติ** | `bg-zinc-700` | - | `text-zinc-500` | `text-blue-400` |
| **คำเตือน** | `bg-yellow-900/20` | `border-yellow-500/50` | `text-yellow-400` | `text-blue-400` |
| **ผิดพลาด** | `bg-red-900/30` | `border-red-500` | `text-red-400` | `text-red-400` |

---

## 💡 User Experience Improvements

### Before:
1. ❌ ไม่รู้ว่าต้องอัปโหลดรูปหรือไม่
2. ❌ ข้อกำหนดไฟล์ไม่เด่น อาจมองข้าม
3. ❌ ไฟล์ขนาด 4.8MB แสดงเป็น "4915.2 KB" - อ่านยาก
4. ❌ ไม่มี visual feedback เมื่อไฟล์ใกล้เกินขนาด

### After:
1. ✅ ระบุชัดเจนว่า "(ไม่บังคับ)"
2. ✅ ข้อกำหนดไฟล์อยู่ในกล่องสีฟ้าเด่นชัด
3. ✅ ไฟล์ขนาด 4.8MB แสดงเป็น "4.80 MB" - อ่านง่าย
4. ✅ มี visual indicators (สีเหลือง/แดง) เตือนทันที

---

## 🔧 Technical Details

### File Size Calculation:
```typescript
const fileSizeMB = file.size / 1024 / 1024;
const isLargeFile = fileSizeMB > 4;    // > 4MB แต่ <= 5MB
const isTooLarge = fileSizeMB > 5;     // > 5MB
```

### Conditional Styling:
```typescript
className={`
  ${isTooLarge
    ? 'bg-red-900/30 border border-red-500'      // เกิน 5MB
    : isLargeFile
    ? 'bg-yellow-900/20 border border-yellow-500/50'  // 4-5MB
    : 'bg-zinc-700'                              // < 4MB
  }
`}
```

---

## 📱 Responsive Design

การแสดงผลปรับตามขนาดหน้าจอ:
- **Mobile**: แสดงไฟล์ทีละรายการ แบบเต็มความกว้าง
- **Desktop**: แสดงข้อมูลครบถ้วน พร้อม visual indicators

---

## ✅ Testing Checklist

- [x] แสดงข้อกำหนดไฟล์ชัดเจน
- [x] ระบุว่า "(ไม่บังคับ)"
- [x] แสดงขนาดไฟล์เป็น KB/MB ตามความเหมาะสม
- [x] แสดงสีเหลืองเมื่อไฟล์ > 4MB
- [x] แสดงสีแดงเมื่อไฟล์ > 5MB
- [x] แสดงข้อความ "- เกินขนาด!" เมื่อเกิน 5MB
- [x] ปุ่มลบไฟล์ทำงานได้
- [x] รองรับ multiple files
- [x] แสดง error messages จาก validation

---

## 🎯 Impact

### ก่อนปรับปรุง:
- ผู้ใช้อาจไม่ทราบข้อจำกัดขนาดไฟล์
- อาจอัปโหลดไฟล์ขนาดใหญ่แล้วเกิด error
- ไม่มี feedback ทันทีว่าไฟล์ใหญ่เกินไป

### หลังปรับปรุง:
- ✅ ผู้ใช้เห็นข้อจำกัดชัดเจนก่อนอัปโหลด
- ✅ มี visual warning เมื่อไฟล์ใกล้เกินขนาด
- ✅ เห็น error ทันทีเมื่อไฟล์เกิน 5MB
- ✅ ประสบการณ์ที่ดีขึ้น ลด frustration

---

## 📚 Related Files

- [src/app/partner/apply/components/GymDetailsForm.tsx](../src/app/partner/apply/components/GymDetailsForm.tsx) - UI Component (แก้ไขแล้ว)
- [src/lib/utils/file-validation.ts](../src/lib/utils/file-validation.ts) - Validation Logic
- [src/app/partner/apply/hooks/useFileUpload.ts](../src/app/partner/apply/hooks/useFileUpload.ts) - Upload Hook
- [docs/FILE_UPLOAD_LIMITS.md](./FILE_UPLOAD_LIMITS.md) - File Upload Documentation

---

## 🎉 Summary

**ปรับปรุง UI ให้**:
1. ✅ ข้อกำหนดไฟล์ชัดเจน มองเห็นง่าย
2. ✅ แสดงขนาดไฟล์ที่อ่านง่าย (KB/MB)
3. ✅ Visual indicators สำหรับไฟล์ใหญ่/เกินขนาด
4. ✅ ระบุว่าการอัปโหลดรูปไม่บังคับ

**ผลลัพธ์**:
- 🎯 ผู้ใช้เข้าใจข้อจำกัดได้ชัดเจน
- 🎯 ลดโอกาสที่จะอัปโหลดไฟล์ผิดประเภทหรือใหญ่เกินไป
- 🎯 ประสบการณ์การใช้งานที่ดีขึ้น
