# Internationalization (i18n) Documentation

**โครงสร้างและคู่มือการใช้งานระบบ i18n สำหรับ 3 ภาษา: ไทย, อังกฤษ, ญี่ปุ่น**

---

## 📑 สารบัญ

1. [ภาพรวม](#ภาพรวม)
2. [ไฟล์เอกสาร](#ไฟล์เอกสาร)
3. [โครงสร้างไฟล์](#โครงสร้างไฟล์)
4. [เริ่มต้นใช้งาน](#เริ่มต้นใช้งาน)
5. [การตรวจสอบ](#การตรวจสอบ)
6. [เพิ่มคำแปลใหม่](#เพิ่มคำแปลใหม่)
7. [Best Practices](#best-practices)

---

## ภาพรวม

โปรเจคนี้รองรับ **3 ภาษา** โดยใช้ [next-intl](https://next-intl-docs.vercel.app/) v4.4.0:

| ภาษา | Locale Code | Flag | URL Pattern |
|------|-------------|------|-------------|
| **ภาษาไทย** (เริ่มต้น) | `th` | 🇹🇭 | `/th/...` |
| **ภาษาอังกฤษ** | `en` | 🇬🇧 | `/en/...` |
| **ภาษาญี่ปุ่น** | `jp` | 🇯🇵 | `/jp/...` |

### สถิติปัจจุบัน
- 📊 **จำนวนคำแปล:** 83 keys ต่อภาษา
- 📁 **ไฟล์แปล:** 3 ไฟล์ (th.json, en.json, jp.json)
- ✅ **สถานะ:** ผ่านการตรวจสอบทั้งหมด

---

## ไฟล์เอกสาร

| ไฟล์ | วัตถุประสงค์ | เหมาะสำหรับ |
|------|-------------|------------|
| **[I18N_README.md](./I18N_README.md)** | เอกสารหลักและจุดเริ่มต้น | ทุกคน |
| **[I18N_QUICK_REFERENCE.md](./I18N_QUICK_REFERENCE.md)** | คู่มืออ้างอิงด่วน | นักพัฒนา |
| **[I18N_STRUCTURE.md](./I18N_STRUCTURE.md)** | โครงสร้างแบบละเอียด | นักพัฒนา/สถาปนิก |
| **[I18N_TEMPLATE.md](./I18N_TEMPLATE.md)** | Template สำหรับเพิ่มคำแปล | นักพัฒนา/แปล |

### คำแนะนำการใช้เอกสาร

1. **เริ่มต้นใช้งาน?** → อ่าน [I18N_QUICK_REFERENCE.md](./I18N_QUICK_REFERENCE.md)
2. **ต้องการเพิ่มคำแปล?** → ใช้ [I18N_TEMPLATE.md](./I18N_TEMPLATE.md)
3. **ต้องการเข้าใจโครงสร้าง?** → อ่าน [I18N_STRUCTURE.md](./I18N_STRUCTURE.md)
4. **ต้องการภาพรวม?** → อ่านไฟล์นี้ (I18N_README.md)

---

## โครงสร้างไฟล์

```
muaythai-next-postgres/
│
├── messages/                      # ไฟล์แปลภาษา
│   ├── th.json                    # ภาษาไทย (83 keys)
│   ├── en.json                    # ภาษาอังกฤษ (83 keys)
│   └── jp.json                    # ภาษาญี่ปุ่น (83 keys)
│
├── src/
│   ├── i18n.ts                    # คอนฟิกหลัก i18n
│   ├── middleware.ts              # Middleware สำหรับ routing
│   ├── navigation.ts              # Navigation helpers
│   └── components/
│       └── shared/
│           └── LanguageSwitcher.tsx  # ตัวเปลี่ยนภาษา
│
├── scripts/
│   └── validate-i18n.js           # สคริปต์ตรวจสอบความถูกต้อง
│
└── docs/
    ├── I18N_README.md             # ไฟล์นี้
    ├── I18N_QUICK_REFERENCE.md    # คู่มืออ้างอิงด่วน
    ├── I18N_STRUCTURE.md          # โครงสร้างละเอียด
    └── I18N_TEMPLATE.md           # Template คำแปล
```

---

## เริ่มต้นใช้งาน

### 1. ใช้งานในคอมโพเนนต์

```tsx
import { useTranslations } from 'next-intl';

export default function MyComponent() {
  const t = useTranslations();

  return (
    <div>
      <h1>{t('navigation.home')}</h1>
      <button>{t('common.buttons.save')}</button>
      <p>{t('common.messages.loading')}</p>
    </div>
  );
}
```

### 2. ใช้งานกับ Namespace

```tsx
const t = useTranslations('auth.login');

return (
  <form>
    <h1>{t('title')}</h1>
    <p>{t('subtitle')}</p>
    <button>{t('button')}</button>
  </form>
);
```

### 3. ใช้งานกับ Dynamic Values

```tsx
const t = useTranslations('validation');

// ใช้ {variable} placeholder
<p>{t('required', { field: 'อีเมล' })}</p>
<p>{t('passwordTooShort', { min: 8 })}</p>
```

### 4. เปลี่ยนภาษา

```tsx
import { useLocale, usePathname } from 'next-intl';

const locale = useLocale();
const pathname = usePathname();

const switchLanguage = (newLocale: string) => {
  const pathWithoutLocale = pathname.replace(`/${locale}`, '');
  window.location.href = `/${newLocale}${pathWithoutLocale}`;
};

// เปลี่ยนเป็นภาษาอังกฤษ
switchLanguage('en');
```

**หรือใช้ Component สำเร็จรูป:**
```tsx
import { LanguageSwitcher } from '@/components/shared/LanguageSwitcher';

<LanguageSwitcher />
```

---

## การตรวจสอบ

### ตรวจสอบความถูกต้องของไฟล์แปล

```bash
# รันสคริปต์ตรวจสอบ
node scripts/validate-i18n.js
```

### ผลลัพธ์ที่คาดหวัง

```
============================================================
  🌐 I18n Validation Script
============================================================

📂 กำลังอ่านไฟล์แปล...
   ✓ อ่านไฟล์เสร็จสิ้น

🔍 กำลังตรวจสอบ keys...
   📝 TH keys: 83
   📝 EN keys: 83
   📝 JP keys: 83

🔎 ตรวจสอบความสอดคล้องของ keys...
   ✓ Keys ทั้งหมดตรงกัน

🔎 ตรวจสอบค่าว่าง...
   ✓ ไม่มีค่าว่าง

🔎 ตรวจสอบ JSON format...
   ✓ JSON format ถูกต้องทั้งหมด

============================================================
  ✅ ผ่านการตรวจสอบทั้งหมด!
============================================================
```

### สคริปต์ตรวจสอบอะไรบ้าง?

1. ✅ **Keys Consistency** - ตรวจสอบว่า keys ในทั้ง 3 ภาษาตรงกัน
2. ✅ **Empty Values** - ตรวจสอบค่าว่าง (null, undefined, "")
3. ✅ **JSON Format** - ตรวจสอบ syntax JSON ถูกต้อง
4. ✅ **Missing Keys** - แสดง keys ที่ขาดหายในแต่ละภาษา
5. ✅ **Extra Keys** - แสดง keys ที่เกินมาในแต่ละภาษา

---

## เพิ่มคำแปลใหม่

### ขั้นตอน 5 ขั้น

#### 1. เลือก Namespace ที่เหมาะสม

| Namespace | ใช้สำหรับ |
|-----------|-----------|
| `common.buttons` | ปุ่มต่างๆ |
| `common.labels` | ป้ายข้อมูล |
| `common.status` | สถานะ |
| `common.messages` | ข้อความแจ้งเตือน |
| `navigation` | เมนูนำทาง |
| `auth.*` | ระบบล็อกอิน/สมัคร |
| `dashboard` | แดชบอร์ด |
| `validation` | การตรวจสอบ |
| **สร้างใหม่** | ฟีเจอร์เฉพาะ (เช่น `booking`, `payment`) |

#### 2. เพิ่มใน 3 ไฟล์พร้อมกัน

**messages/th.json**
```json
{
  "booking": {
    "title": "จองคอร์ส",
    "selectDate": "เลือกวันที่",
    "confirm": "ยืนยันการจอง"
  }
}
```

**messages/en.json**
```json
{
  "booking": {
    "title": "Book a Course",
    "selectDate": "Select Date",
    "confirm": "Confirm Booking"
  }
}
```

**messages/jp.json**
```json
{
  "booking": {
    "title": "コースを予約",
    "selectDate": "日付を選択",
    "confirm": "予約を確認"
  }
}
```

#### 3. ตรวจสอบ JSON Syntax

```bash
# ตรวจสอบแต่ละไฟล์
cat messages/th.json | python3 -m json.tool > /dev/null && echo "✓ TH OK"
cat messages/en.json | python3 -m json.tool > /dev/null && echo "✓ EN OK"
cat messages/jp.json | python3 -m json.tool > /dev/null && echo "✓ JP OK"
```

#### 4. รันสคริปต์ตรวจสอบ

```bash
node scripts/validate-i18n.js
```

#### 5. ทดสอบในแอป

```tsx
const t = useTranslations('booking');
console.log(t('title')); // จองคอร์ส (ถ้าเป็น th)
```

### ใช้ Template

ดู template พร้อมใช้งานใน [I18N_TEMPLATE.md](./I18N_TEMPLATE.md):
- ฟอร์ม (Form)
- ตาราง (Table)
- Modal/Dialog
- ระบบจอง (Booking)
- ระบบชำระเงิน (Payment)
- โปรไฟล์ผู้ใช้ (Profile)
- รีวิว (Reviews)
- และอื่นๆ

---

## Best Practices

### ✅ ทำ (DO)

1. **ใช้ Namespace** - จัดกลุ่ม keys ให้เป็นหมวดหมู่
   ```tsx
   const t = useTranslations('auth.login');
   ```

2. **ใช้ Dynamic Values** - สำหรับข้อความที่มีตัวแปร
   ```tsx
   t('welcome', { name: 'John' })
   ```

3. **ตรวจสอบทุกครั้ง** - รัน validation script ก่อน commit
   ```bash
   node scripts/validate-i18n.js
   ```

4. **เพิ่มครบทั้ง 3 ภาษา** - อัปเดตพร้อมกัน
   ```bash
   # แก้ไขทั้ง 3 ไฟล์
   code messages/th.json messages/en.json messages/jp.json
   ```

5. **ใช้ชื่อที่สื่อความหมาย** - camelCase และชัดเจน
   ```json
   "confirmDelete": "...",  // ✅ Good
   "cd": "...",             // ❌ Bad
   ```

### ❌ ไม่ทำ (DON'T)

1. **Hardcode ข้อความ** - อย่าเขียนข้อความตรงๆ
   ```tsx
   <button>บันทึก</button>           // ❌ Bad
   <button>{t('common.buttons.save')}</button>  // ✅ Good
   ```

2. **เพิ่มเฉพาะบางภาษา** - ต้องครบทั้ง 3
   ```bash
   # ❌ Bad - เพิ่มเฉพาะ TH
   # ✅ Good - เพิ่มทั้ง TH, EN, JP
   ```

3. **ซ้ำ keys** - ตรวจสอบก่อนเพิ่มใหม่
   ```bash
   cat messages/th.json | grep "title"
   ```

4. **ใช้ key ยาวเกินไป** - ควรกระชับ
   ```json
   "auth.login.form.email.input.placeholder": "..."  // ❌ Too long
   "auth.login.email": "..."                         // ✅ Better
   ```

---

## หมวดหมู่คำแปล

### ภาพรวม Namespaces

```
common (41 keys)
├── buttons (18 keys)    - save, cancel, delete, edit, ...
├── labels (10 keys)     - email, password, name, ...
├── status (7 keys)      - active, pending, completed, ...
└── messages (6 keys)    - loading, success, error, ...

navigation (13 keys)     - home, gyms, events, ...

auth (18 keys)
├── login (7 keys)       - title, email, button, ...
├── signup (8 keys)      - fullName, confirmPassword, ...
└── forgotPassword (3)   - title, subtitle, button

dashboard (4 keys)       - welcome, overview, ...

validation (5 keys)      - required, invalidEmail, ...
```

### Keys ที่ใช้บ่อยที่สุด

```tsx
// Top 10 Most Used Keys
t('common.buttons.save')           // บันทึก
t('common.buttons.cancel')         // ยกเลิก
t('common.messages.loading')       // กำลังโหลด...
t('common.messages.success')       // สำเร็จ
t('common.messages.error')         // เกิดข้อผิดพลาด
t('navigation.home')               // หน้าแรก
t('navigation.dashboard')          // แดชบอร์ด
t('common.status.active')          // ใช้งาน
t('common.status.pending')         // รอดำเนินการ
t('validation.required')           // กรุณากรอก{field}
```

---

## Troubleshooting

### ปัญหาที่พบบ่อย

#### 1. แสดง Key แทนข้อความ

**อาการ:** แสดง "navigation.home" แทนที่จะเป็น "หน้าแรก"

**วิธีแก้:**
```bash
# ตรวจสอบว่า key มีอยู่
cat messages/th.json | grep -A 2 "navigation"

# ตรวจสอบ spelling
# ตรวจสอบ JSON syntax
node scripts/validate-i18n.js
```

#### 2. เปลี่ยนภาษาไม่ได้

**อาการ:** คลิกเปลี่ยนภาษาแล้วไม่เปลี่ยน

**วิธีแก้:**
- ใช้ `window.location.href` แทน `router.push`
- Clear browser cache
- ตรวจสอบ URL pattern (`/th/`, `/en/`, `/jp/`)

#### 3. JSON Syntax Error

**อาการ:** Error ตอนรันแอป

**วิธีแก้:**
```bash
# ตรวจสอบแต่ละไฟล์
cat messages/th.json | python3 -m json.tool
cat messages/en.json | python3 -m json.tool
cat messages/jp.json | python3 -m json.tool

# หรือรัน validation
node scripts/validate-i18n.js
```

#### 4. Keys ไม่ตรงกัน

**อาการ:** บางภาษามี key ที่ภาษาอื่นไม่มี

**วิธีแก้:**
```bash
# รัน validation จะแสดงรายการ keys ที่ไม่ตรงกัน
node scripts/validate-i18n.js
```

---

## npm Scripts (แนะนำ)

เพิ่มใน `package.json`:

```json
{
  "scripts": {
    "validate:i18n": "node scripts/validate-i18n.js",
    "i18n:check": "node scripts/validate-i18n.js",
    "precommit": "npm run validate:i18n"
  }
}
```

ใช้งาน:
```bash
npm run validate:i18n
npm run i18n:check
```

---

## Links & Resources

### เอกสารโปรเจค
- [I18N Quick Reference](./I18N_QUICK_REFERENCE.md) - คู่มืออ้างอิงด่วน
- [I18N Structure](./I18N_STRUCTURE.md) - โครงสร้างละเอียด
- [I18N Template](./I18N_TEMPLATE.md) - Template สำหรับเพิ่มคำแปล

### ไฟล์สำคัญ
- [messages/th.json](../messages/th.json) - ไฟล์แปลภาษาไทย
- [messages/en.json](../messages/en.json) - ไฟล์แปลภาษาอังกฤษ
- [messages/jp.json](../messages/jp.json) - ไฟล์แปลภาษาญี่ปุ่น
- [src/i18n.ts](../src/i18n.ts) - คอนฟิก i18n
- [scripts/validate-i18n.js](../scripts/validate-i18n.js) - สคริปต์ตรวจสอบ

### เอกสารภายนอก
- [next-intl Documentation](https://next-intl-docs.vercel.app/)
- [Next.js Internationalization](https://nextjs.org/docs/advanced-features/i18n-routing)

---

## Quick Command Reference

```bash
# ตรวจสอบความถูกต้อง
node scripts/validate-i18n.js

# ดูโครงสร้าง JSON
cat messages/th.json | python3 -m json.tool

# ค้นหา key
cat messages/th.json | grep "navigation"

# นับจำนวน keys
cat messages/th.json | grep -o '":' | wc -l

# เปรียบเทียบ keys
diff <(cat messages/th.json | python3 -c "import sys,json; print('\n'.join(json.load(sys.stdin).keys()))") \
     <(cat messages/en.json | python3 -c "import sys,json; print('\n'.join(json.load(sys.stdin).keys()))")
```

---

## Contributing

เมื่อเพิ่มคำแปลใหม่:

1. ✅ เพิ่มใน 3 ไฟล์พร้อมกัน (th.json, en.json, jp.json)
2. ✅ รัน `node scripts/validate-i18n.js`
3. ✅ ทดสอบในแอป
4. ✅ Commit พร้อม message ที่ชัดเจน
5. ✅ สร้าง Pull Request

### Commit Message Format

```bash
git commit -m "Add i18n translations for [feature]

- Added translations for booking system
- Updated common.buttons with new actions
- All 3 languages (th, en, jp) included
"
```

---

## Summary

| รายการ | รายละเอียด |
|--------|-----------|
| **ภาษา** | ไทย (th), อังกฤษ (en), ญี่ปุ่น (jp) |
| **จำนวน Keys** | 83 keys ต่อภาษา |
| **ไลบรารี** | next-intl v4.4.0 |
| **URL Pattern** | `/[locale]/...` |
| **ภาษาเริ่มต้น** | ไทย (th) |
| **สถานะ** | ✅ ผ่านการตรวจสอบ |
| **อัปเดตล่าสุด** | 2025-11-06 |

---

## Contact

หากมีคำถามหรือต้องการความช่วยเหลือ:
1. อ่านเอกสารใน `docs/` directory
2. ตรวจสอบ [I18N_QUICK_REFERENCE.md](./I18N_QUICK_REFERENCE.md)
3. รัน validation script
4. ดูตัวอย่างใน [I18N_TEMPLATE.md](./I18N_TEMPLATE.md)

---

**สร้างเมื่อ:** 2025-11-06
**เวอร์ชัน:** 1.0.0
**ผู้ดูแล:** Development Team
