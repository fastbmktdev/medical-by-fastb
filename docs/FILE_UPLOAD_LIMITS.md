# File Upload Limits - Partner Application

## 📋 สรุปข้อจำกัดการอัปโหลดไฟล์

### 🎯 การอัปโหลดรูปภาพยิม / โลโก้

#### ข้อจำกัดปัจจุบัน:

| รายการ | ค่าที่กำหนด |
|--------|-------------|
| **ประเภทไฟล์ที่รองรับ** | JPG, JPEG, PNG, WebP |
| **ขนาดไฟล์สูงสุด** | 5 MB ต่อไฟล์ |
| **จำนวนไฟล์** | ไม่จำกัด (แนะนำ 3-5 ไฟล์) |
| **ความกว้าง/สูงแนะนำ** | 800x600 px ขึ้นไป |

---

## 🔒 ระบบรักษาความปลอดภัย

### การตรวจสอบไฟล์ที่ครอบคลุม:

1. **ตรวจสอบขนาดไฟล์**
   - ไฟล์ที่เกิน 5MB จะถูกปฏิเสธ
   - ไฟล์ว่างเปล่า (0 bytes) จะถูกปฏิเสธ

2. **ตรวจสอบประเภทไฟล์**
   - ตรวจสอบ MIME type
   - ตรวจสอบนามสกุลไฟล์
   - ตรวจสอบ Magic Bytes (เนื้อหาจริงของไฟล์)

3. **ตรวจสอบความปลอดภัย**
   - บล็อกไฟล์ที่อันตราย (.exe, .bat, .php, etc.)
   - ตรวจสอบเนื้อหาที่น่าสงสัย (script tags, eval(), etc.)
   - Sanitize ชื่อไฟล์ (ป้องกัน directory traversal)

4. **การตรวจสอบเพิ่มเติม**
   - ตรวจสอบว่าไฟล์ไม่ใช่ malware
   - ตรวจสอบ magic bytes เพื่อยืนยันว่าเป็นไฟล์รูปภาพจริง
   - ตรวจสอบการปลอมแปลงประเภทไฟล์

---

## 📝 ไฟล์ที่เกี่ยวข้อง

### 1. File Validation
**Path**: `src/lib/utils/file-validation.ts`

```typescript
export const FILE_TYPE_CONFIGS: Record<string, FileTypeConfig> = {
  image_jpeg: {
    mimeTypes: ['image/jpeg', 'image/jpg'],
    extensions: ['jpg', 'jpeg'],
    magicBytes: [[0xFF, 0xD8, 0xFF]],
    maxSize: 5 * 1024 * 1024, // 5MB
  },
  image_png: {
    mimeTypes: ['image/png'],
    extensions: ['png'],
    magicBytes: [[0x89, 0x50, 0x4E, 0x47, 0x0D, 0x0A, 0x1A, 0x0A]],
    maxSize: 5 * 1024 * 1024, // 5MB
  },
  image_webp: {
    mimeTypes: ['image/webp'],
    extensions: ['webp'],
    magicBytes: [[0x52, 0x49, 0x46, 0x46]],
    maxSize: 5 * 1024 * 1024, // 5MB
  },
};
```

### 2. Client-Side Validation
**Path**: `src/app/partner/apply/hooks/useFileUpload.ts`

- เรียกใช้ `validateFile()` เพื่อตรวจสอบไฟล์ก่อนอัปโหลด
- แสดง error messages ทันทีเมื่อเลือกไฟล์ที่ไม่ถูกต้อง

### 3. UI Display
**Path**: `src/app/partner/apply/components/GymDetailsForm.tsx`

```tsx
<p className="mb-3 text-zinc-500 text-xs">
  รองรับไฟล์: JPG, PNG (ขนาดไม่เกิน 5MB ต่อไฟล์)
</p>
```

### 4. File Upload
**Path**: `src/app/partner/apply/utils/fileUpload.ts`

- จัดการการอัปโหลดไฟล์ไปยัง Supabase Storage
- Sanitize ชื่อไฟล์เพื่อความปลอดภัย
- Error handling ที่ครอบคลุม

---

## ✅ User Experience

### การแสดงข้อความ:

1. **ก่อนอัปโหลด**
   - แสดงข้อจำกัดขนาดไฟล์และประเภทไฟล์ที่รองรับ
   - แสดง drag & drop zone

2. **ระหว่างเลือกไฟล์**
   - ตรวจสอบไฟล์ทันที (client-side validation)
   - แสดง error messages หากไฟล์ไม่ถูกต้อง

3. **หลังเลือกไฟล์**
   - แสดงรายการไฟล์ที่เลือก
   - แสดงขนาดไฟล์ (KB/MB)
   - แสดงปุ่มลบไฟล์

4. **หลังส่งฟอร์ม**
   - แสดง loading state ระหว่างอัปโหลด
   - แสดง success/error message

---

## 🚀 การปรับปรุงที่แนะนำ (Optional)

### 1. เพิ่มการบีบอัดรูปภาพอัตโนมัติ
```typescript
// Image compression before upload
import imageCompression from 'browser-image-compression';

const options = {
  maxSizeMB: 1,
  maxWidthOrHeight: 1920,
  useWebWorker: true
};

const compressedFile = await imageCompression(file, options);
```

### 2. เพิ่ม Image Preview
```tsx
<Image
  src={URL.createObjectURL(file)}
  alt="preview"
  className="w-20 h-20 object-cover rounded"
/>
```

### 3. Progress Bar สำหรับการอัปโหลด
```typescript
const { progress } = await supabase.storage
  .from('gym-images')
  .upload(fileName, file, {
    onUploadProgress: (progress) => {
      const percent = (progress.loaded / progress.total) * 100;
      console.log(`Upload progress: ${percent}%`);
    }
  });
```

### 4. Multiple File Upload with Batch Processing
```typescript
// Upload files in batches to avoid timeout
const BATCH_SIZE = 3;
for (let i = 0; i < files.length; i += BATCH_SIZE) {
  const batch = files.slice(i, i + BATCH_SIZE);
  await Promise.all(batch.map(file => uploadFile(file)));
}
```

---

## 📊 Error Messages

### ข้อความ Error ที่ผู้ใช้อาจพบ:

| Error | สาเหตุ | แนวทางแก้ไข |
|-------|--------|-------------|
| "ขนาดไฟล์เกิน 5.0MB" | ไฟล์ใหญ่เกินไป | ลดขนาดไฟล์หรือบีบอัดรูปภาพ |
| "ประเภทไฟล์ไม่ถูกต้อง" | ไฟล์ไม่ใช่ JPG/PNG | ใช้ไฟล์รูปภาพที่ถูกต้อง |
| "ไฟล์ว่างเปล่า" | ไฟล์มีขนาด 0 bytes | เลือกไฟล์ที่ถูกต้อง |
| "นามสกุลไฟล์ไม่ถูกต้อง" | นามสกุลไม่ใช่ .jpg, .png | เปลี่ยนนามสกุลไฟล์ |
| "เนื้อหาไฟล์ไม่ตรงกับประเภท" | ไฟล์ถูกปลอมแปลง | ใช้ไฟล์รูปภาพจริง |
| "การอัปโหลดรูปภาพล้มเหลว" | Storage error หรือ network | ลองใหม่อีกครั้ง |

---

## 🔧 Configuration

### เปลี่ยนขนาดไฟล์สูงสุด:

**ไฟล์**: `src/lib/utils/file-validation.ts`

```typescript
// เปลี่ยนจาก 5MB เป็น 10MB
maxSize: 10 * 1024 * 1024, // 10MB
```

**อย่าลืม**: อัปเดตข้อความใน UI ด้วย!

**ไฟล์**: `src/app/partner/apply/components/GymDetailsForm.tsx`

```tsx
<p className="mb-3 text-zinc-500 text-xs">
  รองรับไฟล์: JPG, PNG (ขนาดไม่เกิน 10MB ต่อไฟล์)
</p>
```

---

## 📚 References

- [File Validation Code](../src/lib/utils/file-validation.ts)
- [File Upload Code](../src/app/partner/apply/utils/fileUpload.ts)
- [Upload Hook](../src/app/partner/apply/hooks/useFileUpload.ts)
- [UI Component](../src/app/partner/apply/components/GymDetailsForm.tsx)
- [Supabase Storage Setup](./SUPABASE_STORAGE_SETUP.md)

---

## ✅ Summary

**ระบบปัจจุบัน**:
- ✅ จำกัดขนาดไฟล์ 5MB ต่อไฟล์
- ✅ รองรับเฉพาะ JPG, PNG, WebP
- ✅ ตรวจสอบความปลอดภัยครอบคลุม
- ✅ แสดงข้อความจำกัดใน UI
- ✅ Validation ทั้ง client และ server side
- ✅ Error handling ที่ดี

**การใช้งาน**:
1. ผู้ใช้เห็นข้อจำกัดชัดเจนก่อนอัปโหลด
2. ระบบตรวจสอบไฟล์ทันทีเมื่อเลือก
3. แสดง error หากไฟล์ไม่ผ่านเกณฑ์
4. อนุญาตให้อัปโหลดเฉพาะไฟล์ที่ถูกต้อง
