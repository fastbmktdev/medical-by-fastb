# Supabase Configuration
# Get these from your Supabase project settings at https://app.supabase.com

# NEXT_PUBLIC_* variables are exposed to the browser (safe for anon key)
# Required for client-side authentication
NEXT_PUBLIC_SUPABASE_URL=your-project-url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Regular env vars are server-side only (NOT exposed to browser)
# Required for server-side operations
SUPABASE_URL=your-project-url
SUPABASE_ANON_KEY=your-anon-key
