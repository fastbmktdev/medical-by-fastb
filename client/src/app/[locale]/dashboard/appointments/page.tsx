"use client";

/**
 * Appointments Page - Re-export bookings page
 * This page exists for backward compatibility with menu items and links
 * that reference /dashboard/appointments
 * 
 * Uses the same component as /dashboard/bookings
 */
export { default } from "../bookings/page";

